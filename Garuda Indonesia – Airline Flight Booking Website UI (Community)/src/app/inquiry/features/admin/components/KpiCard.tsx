export function Kpi({ title, value, unit, trend, tone, icon }: { title: string; value: string | number; unit: string; trend: string; tone: string; icon: string }) { return <div className="kpi-card"><div className={`kpi-icon ${tone}`}>{icon}</div><div><span>{title}</span><strong>{value}<small>{unit}</small></strong><p className={tone === "amber" ? "warning" : ""}>{trend}</p></div></div>; }
